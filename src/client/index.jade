html
  head
    base(href='/')
    meta(charset='utf-8')
    title vs-agency
    meta(name='description', content='')
    meta(name="viewport" content="user-scalable=no, width=device-width, initial-scale=1, maximum-scale=1")
  body.property-hub(ng-app='vs-agency', tap='mobileMenuOut=false')
    header
    mobile-menu
    .holder(ng-class='{"mobile-menu-out":mobileMenuOut}')
      .body
        .container
          login
          .view(ui-view='', ng-show='auth.getUser()')
    footer
    progression-popup
    date-swiper(config='{prefix:"swiper", modifier:1.5, show:false}')
    script(src='https://use.fontawesome.com/7cf3591d99.js')